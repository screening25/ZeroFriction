# 변경 이력 (CHANGELOG)

> Zero-Friction(Zero-Scheduler)의 변경 사항을 **버전별**로, 주요 항목은 **변경 전(Before) → 변경 후(After)** 형식으로 기록합니다.
> 버전 구분은 git 히스토리(커밋 19개, 2026-05-19 ~ 05-21)를 분석해 **논리적 마일스톤**으로 재구성한 것이며, 아직 git 태그는 부여하지 않았습니다.
> 형식은 [Keep a Changelog](https://keepachangelog.com/ko/1.1.0/) · [유의적 버전(SemVer)](https://semver.org/lang/ko/)을 따릅니다. 커밋 규칙은 [`CONTRIBUTING.md`](./CONTRIBUTING.md) 참고.

---

## 버전 개요

| 버전 | 날짜 | 핵심 요약 | 대표 커밋 |
| --- | --- | --- | --- |
| **v0.8.2** | 2026-06-08 | 고객사 UI를 태그 → 세로 리스트로 변경 | `최신` |
| **v0.8.1** | 2026-06-08 | 재고 그룹 접기/펴기(그룹 헤더 토글) | — |
| **v0.8.0** | 2026-06-08 | 품목코드 필수화, 총재고/입고/출고 뷰, 그룹내 항목 드래그, 동기화 버튼 설정 이동, 고객사 플레이스홀더 제거 | — |
| **v0.7.9** | 2026-06-08 | 재고 순서 드래그(☰ 핸들)로 직접 변경·영속 | — |
| **v0.7.8** | 2026-06-08 | 재고 입출고 이력(트랜잭션 로그) 기록·조회, page.tsx 순수 헬퍼 분리 | — |
| **v0.7.7** | 2026-06-08 | 일정·재고·메모 통합 검색 기능, 데스크톱 앱 보안 강화(Electron contextIsolation) | — |
| **v0.7.6** | 2026-06-08 | 고객사 등록 위치를 재고 설정 → 일정 카테고리 설정으로 이동 | — |
| **v0.7.5** | 2026-06-08 | 재고 건전성·브리핑 위험 기준을 음수(qty<0)로 통일 | — |
| **v0.7.4** | 2026-06-08 | 업데이트 캐시버스팅(업데이트 안 되던 문제), 고객사 입력칸 항상 표시 | — |
| **v0.7.3** | 2026-06-08 | 다크모드 메모 모달 투명 배경 버그 수정 (가독성) | — |
| **v0.7.2** | 2026-06-08 | 고객사 연동을 재고·메모로 확장 | — |
| **v0.7.1** | 2026-06-08 | 재고 그룹화 메인 화면 실제 적용, 다크모드 메모 가독성 개선 | — |
| **v0.7.0** | 2026-06-08 | 재고 품목코드별 그룹화·사이즈 분류, 위험재고 정의, 메모 순서 고정, 고객사 관리·일정 연동, 헤더 동기화 버튼 | — |
| **v0.6.2** | 2026-05-29 | 일정·재고·메모 복제 기능 추가, 설정 내 앱 업데이트 버튼 추가 | — |
| **v0.6.1** | 2026-05-29 | OS 배너 알림 지원, 헤더 업데이트 버튼, 컴퓨터·폰 데이터 동기화 | — |
| **v0.6.0** | 2026-05-29 | Vercel 배포 + Neon DB 연결, Android APK 빌드, 버그 수정 | — |
| **v0.5.10** | 2026-05-22 | 재고 일괄 등록 시 시리얼 번호 파싱 분류 고도화 및 리스트 페이지 삭제 시 페이징 바운더리 자동 보정 | _(로컬)_ |
| **v0.5.9** | 2026-05-22 | 데이터베이스 중복 ID 자동 복구, 신규 생성 고유화 및 카드 액션 버블링 전면 차단 | _(미배포)_ |
| **v0.5.8** | 2026-05-22 | 연속 삭제 조작성 및 토스트 차단 문제 완전 해결 | _(미배포)_ |
| **v0.5.7** | 2026-05-22 | 토스트 알림 클릭 패스스루 및 재고 일괄 등록 Markdown 파싱 & 중복 병합 추가 | _(미배포)_ |
| **v0.5.6** | 2026-05-22 | 재고 일괄 등록(Excel 붙여넣기 파싱) 및 통합 메모 생성/상호 연동 기능 추가 | _(미배포)_ |
| **v0.5.5** | 2026-05-22 | 일정 카테고리 색상 선택 UI 리디자인 및 프리미엄 서클 버튼 적용 | _(미배포)_ |
| **v0.5.4** | 2026-05-22 | 일정 카테고리별 커스텀 색상(기본 프리셋 + 컬러 피커 팔레트) 지정 기능 추가 및 UI 동적 렌더링 | _(미배포)_ |
| **v0.5.3** | 2026-05-22 | 일정 등록 시간 입력 UI 일인화 및 상단 바 퀵 액션 단축 패널 추가 | _(미배포)_ |
| **v0.5.2** | 2026-05-22 | macOS 상단 바 트레이 드롭다운 패널 연동 · 퀵 입력 포커스 · 아이콘 디자인 개선 | _(미배포)_ |
| **v0.5.1** | 2026-05-22 | 알림 제시각 표시 보장 · 알림 카드 확인 버튼 추가 · 시간 직접 입력 지원 · 중복 OS 제거 | _(미배포)_ |
| **v0.5.0** | 2026-05-22 | 데스크톱 알림 UI(글래스모피즘 알람·스누즈) · Electron IPC 보강 · 설정 업데이트 내역 페이지네이션 | _(미배포)_ |
| **v0.4.4** | 2026-05-22 | 일정·재고·메모 Excel 및 PDF 내보내기 기능 추가 | `83df12c` |
| **v0.4.3** | 2026-05-22 | 일정 하루 종일 옵션 · 캘린더 점 유지 · 메모 마크다운 표/코드블록 지원 | `eb8760d` `380046d` |
| **v0.4.2** | 2026-05-22 | 시간 선택기 미니멀화(Stepper 전용) · 앱 실행 시 터미널 팝업 버그 수정 | `9693fa4` |
| **v0.4.1** | 2026-05-21 | 재고 삭제 UX 개선 · 일정 등록 기본 알림 정각(0분) 설정 | `7dd12b7` |
| **v0.4.0** | 2026-05-21 | 메모 UX 개선 · 입력 폼 정리 · 문서 정비 | `037bb0e` `dc8f767` `17436d3` |
| **v0.3.0** | 2026-05-21 | 코드 리팩토링 · `src/` 아키텍처 통합 · 마스터 가이드 | `4441c7a` `ec31e38` `1348dcc` |
| **v0.2.0** | 2026-05-20 | 커스텀 UI 컨트롤 · 마크다운 메모 · 화면 고도화 | `ee1cb2d` `6cc0fca` `4f2e447` |
| **v0.1.0** | 2026-05-19 | 초기 구축: 앱 골격 · 백엔드 · Electron · PWA · 알림 | `827a8d5` `71b445e` `a7658df` |

---

## [v0.8.2] — 2026-06-08 · 고객사 UI 태그 → 리스트

### ♻️ 변경

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 고객사 선택(폼) | 입력칸 + 흩어진 태그(뱃지) 버튼 — 개수 많으면 중구난방 | `ClientPicker`를 세로 리스트로 재작성. 체크(✓) 표시가 있는 행 목록에서 선택, 입력값으로 리스트 필터(검색)·직접 입력 겸용, 최대 높이 스크롤 |
| 고객사 관리(설정) | 태그 + × 삭제 | 번호(01·02…)가 매겨진 세로 리스트, 행마다 × 삭제 |

---

## [v0.8.1] — 2026-06-08 · 재고 그룹 접기/펴기

### ✨ 신규 기능

| 항목 | 내용 |
| --- | --- |
| 재고 그룹 접기 | 같은 품목코드 그룹(멀티 사이즈) 헤더를 클릭하면 해당 그룹의 개별 항목을 접거나 펼침. `collapsedGroups`(Set) 상태로 그룹별 독립 토글, 헤더 좌측 ▾/▸ 셰브론으로 상태 표시. 품목 많을 때 큰 틀만 훑고 필요한 그룹만 펼쳐 봄 |

---

## [v0.8.0] — 2026-06-08 · 재고 UX 묶음 개선

### ✨ 신규/변경

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 필수 항목 | 품목명이 필수(`*`), 품목코드 선택 | **품목코드 필수**, 품목명 선택. 저장 검증을 코드 기준으로 변경, 품목명이 비면 코드를 표시명(title)으로 사용해 식별·그룹화 안정 |
| 현황 분리 | 재고가 단일 목록 | 상단에 **총 재고 현황 / 입고 현황 / 출고 현황** 세그먼트(실시간 건수). `attrs.flow` 기준 필터 |
| 그룹 내 정렬 | 그룹(코드) 단위 드래그만 | 같은 그룹 안 **개별 항목(사이즈별)도 ☰ 핸들로 드래그** 재정렬. 같은 코드 내에서만 이동하도록 제한, `sortOrder` 전역 재부여 |
| 헤더 정리 | 헤더에 동기화 버튼 포함(버튼 과다) | **동기화 버튼을 설정 화면으로 이동**('데이터 동기화'). 헤더는 검색·팔레트·활동·설정만 |
| 고객사 입력 | "고객사를 선택하거나 직접 입력하세요." 플레이스홀더 | 플레이스홀더 제거 |

---

## [v0.7.9] — 2026-06-08 · 재고 순서 직접 변경(드래그)

### ✨ 신규 기능

| 항목 | 내용 |
| --- | --- |
| 재고 순서 드래그 정렬 | 각 재고 그룹 카드 왼쪽에 ☰(`Menu`) 드래그 핸들 추가. 잡고 다른 카드 위로 끌어 순서를 재배치하면, 표시 순서대로 모든 항목에 `attrs.sortOrder`를 재부여해 영속화(`inventory`는 `sortOrder` 기준 안정 정렬). 핸들에서 시작한 HTML5 DnD만 동작하며, 카드 본문 클릭(수정)·기존 동작은 그대로 유지 |

### 🐛 구현 메모

| 항목 | 내용 |
| --- | --- |
| stale closure 수정 | 드래그 식별자를 React state로만 두면 드래그 중 `onDrop` 핸들러가 직전 렌더의 클로저(`null`)를 읽어 재정렬이 무시됨. `useRef`(`dragKeyRef`)로 동기 참조하도록 고쳐 해결. 미리보기에서 `[A,B,…] → [B,…,A]` 재정렬·영속 확인 |

> 참고: HTML5 드래그라 데스크톱에서 동작합니다(터치 환경은 추후 보강 대상).

---

## [v0.7.8] — 2026-06-08 · 재고 입출고 이력 · 리팩토링

### ✨ 신규 기능

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 재고 입출고 이력(트랜잭션) | 재고는 순재고 수량(`attrs.qty`)만 들고 있어 어떤 입출고가 있었는지 이력 추적·검증이 불가능했음 | `InventoryTxn` 타입 도입. `addRecord`의 입고/출고마다 `{ flow, qty, balance, loc, mgr, client, memo, ts }`를 `attrs.txns[]`에 **가산 기록**(기존 `qty` 계산·기존 데이터 불변). 재고 수정 모달에 '입출고 이력' 펼침 목록(이동·잔량·담당자·시각) 추가 |

### ♻️ 리팩토링

| 항목 | 내용 |
| --- | --- |
| page.tsx 헬퍼 분리 | 색상·스타일(`hexToRgb`/`getCategoryColorStyles`/`getMemoCardStyle`/`getMemoModalStyle`)·`isHoliday`·`isSerialPattern`를 `src/frontend/utils/*`로 추출, `isHoliday` 중복 정의 단일화(DRY). 각 단계 빌드+스모크 검증 |

> 데이터 안전: 이력은 **가산 방식**이라 기존 재고 데이터를 변형하지 않습니다(마이그레이션 없음). 이력은 이번 배포 이후 발생하는 입출고부터 쌓입니다.

---

## [v0.7.7] — 2026-06-08 · 통합 검색 · 데스크톱 보안 강화

### ✨ 신규 기능

| 항목 | 내용 |
| --- | --- |
| 일정·재고·메모 통합 검색 | 헤더에 돋보기 버튼 추가 → 검색 모드 진입. 직접 입력 검색창 + 타입 탭(전체/일정/재고/메모, 실시간 건수)으로 구성. 매칭 필드를 제목·카테고리·내용·코드·메모에서 **고객사·시리얼·보관위치·담당자·날짜·시간**까지 확장하고, 공백으로 나눈 여러 토큰을 모두 포함하는 AND 검색으로 개선. 기존 NLP(자연어) 검색 인프라(`searchQuery`/`searchType`/`filteredSearchRecords`)를 재사용 |

### 🔒 보안

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| Electron 렌더러 권한 | `nodeIntegration: true` + `contextIsolation: false`로 원격 URL 로드 → XSS 시 OS 권한 탈취(RCE) 위험 | `contextIsolation: true` · `nodeIntegration: false` · `sandbox: true` + `preload.js`로 전환. IPC를 preload 화이트리스트(`window.electronAPI`)로만 노출 |

---

## [v0.7.6] — 2026-06-08 · 고객사 등록 위치 이동 (재고 → 일정)

### ♻️ 변경

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 고객사 목록 등록 위치 | 고객사 추가·삭제('고객사 관리')가 재고 탭의 '기준 정보 설정' 모달 안에 있었음 | 일정 탭의 '일정 카테고리 설정' 모달 하단으로 이동 — 고객사를 주로 일정에서 사용하는 흐름에 맞춤. 등록된 고객사 목록(`appSettings.clients`)은 그대로라 일정·재고·메모 어디서나 동일하게 공유됨 |

---

## [v0.7.5] — 2026-06-08 · 위험 재고 기준 음수(qty<0)로 통일

### 🐛 수정

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 위험/부족 재고 기준 불일치 | 카드 뱃지·그룹 경고는 `qty < 0`(음수)로 바꿨으나, **대시보드 '재고 건전성' 타일**과 아침 브리핑은 여전히 `qty < 5`(안전재고 이하)·`qty === 0`(품절) 기준을 사용 → "위험은 음수일 때"라는 의도와 어긋남 | 모든 위험/부족 판정을 **`qty < 0`(0개 밑으로 떨어진 음수)**로 통일. 건전성 타일은 음수 품목이 있으면 "위험", 없으면 "양호"(중간 '주의' 단계 제거), 브리핑의 "수량 부족 N건"·하위 품목 목록도 음수 기준으로 집계 |

---

## [v0.7.4] — 2026-06-08 · 업데이트 캐시버스팅 · 고객사 입력칸 상시 표시

### 🐛 수정

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| "업데이트가 전혀 안 됨" | 인앱 업데이트 버튼이 SW 캐시·Cache Storage만 비우고 `window.location.reload()`를 호출 — Electron/Chromium의 **HTTP 디스크 캐시는 우회하지 못해** 새 배포가 떠 있어도 캐시된 옛 HTML(→옛 청크)이 다시 로드됨. (배포 자체는 정상: 라이브 청크에 신규 코드 존재 확인됨) | 업데이트/새로고침 시 URL에 `?_v=<timestamp>` 쿼리를 붙여 네트워크에서 **최신 HTML을 강제 재요청**(`location.replace`). Electron `main.js`도 로드 전 `session.clearCache()` + 캐시버스팅 URL 적용, `Cmd/Ctrl+R` 강제 최신화 단축키 추가 |
| 고객사 입력칸이 안 보임 | 고객사 picker가 `appSettings.clients.length > 0`일 때만 표시 → 설정에서 고객사를 등록하기 전엔 일정·재고·메모 어디에도 고객사 칸이 안 나타나 "선택할 게 없음" | 게이팅 제거 — 일정·재고·메모에 고객사 입력칸을 **항상 표시**. 등록된 고객사는 배지로 빠른 선택, 없으면 직접 입력 가능 |

---

## [v0.7.3] — 2026-06-08 · 다크모드 메모 모달 가독성 수정

### 🐛 수정

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 다크모드 메모 모달이 비쳐서 안 보임 | 메모 상세·편집 모달 배경에 `getMemoCardStyle`의 반투명 틴트(rgba 0.05~0.10)만 적용되어, `.modal-content`의 불투명 배경을 덮어써 모달이 사실상 투명 → 뒤 화면(메모 목록 등)이 그대로 비쳐 글자가 안 읽혔음 (특히 다크모드). 실제 측정값: 모달 배경 `rgba(248,113,113,0.1)`, backdrop-filter 없음 | `getMemoModalStyle` 헬퍼 신설 — 불투명 베이스(`var(--bg-color)`) 위에 색상 틴트를 `linear-gradient`로 합성해 모달이 항상 불투명하게 렌더되도록 수정. 측정값: backgroundColor `rgb(0,0,0)` + 틴트 오버레이. 미리보기로 가독성 확인 완료 |

> 참고: v0.7.1의 다크모드 텍스트 색 보정(`--text-secondary #C7C7CC` 등)은 메모 **카드** 본문에는 정상 적용되어 있었고, 실제 "전혀 안 보임"의 주원인은 메모 **모달**의 투명 배경이었습니다.

---

## [v0.7.2] — 2026-06-08 · 고객사 연동 확장 (재고·메모)

### ✨ 신규 기능

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 재고 고객사 연동 | 고객사를 일정에서만 지정할 수 있었음 | 재고 등록·수정 폼에 고객사 선택 섹션 추가(설정에 고객사가 있을 때만 노출). 배지 클릭 또는 직접 입력으로 지정하며, 재고 카드 메타데이터에 고객사 배지 표시 |
| 메모 고객사 연동 | 동일 | 메모 등록·수정 폼에 고객사 선택 섹션 추가. `memoForm.client`로 저장되며 메모 카드·상세보기에 고객사 배지 표시 |

---

## [v0.7.1] — 2026-06-08 · 재고 그룹화 메인 화면 적용 · 다크모드 가독성

### 🐛 수정

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 재고 그룹화가 메인 화면에 안 보임 | 재고 그룹화·위험 재고 표시가 실제로 쓰이지 않는 별도 라우트(`/inventory`)에만 적용되어, 사용자가 보는 메인 재고 화면(`page.tsx`, activeTab 전환)에는 반영되지 않았음 | 메인 재고 화면 렌더링(`page.tsx`)에 동일 품목코드 그룹화·그룹 헤더(코드·종류 수·위험 재고 포함 경고)·개별 위험 재고 뱃지를 직접 이식하여 실제로 표시되도록 수정 |
| 다크모드 메모 가독성 저하 | 다크모드에서 `--text-secondary(#8E8E93)`·`--text-tertiary(#48484A)`가 라이트모드와 동일하거나 너무 어두워, 어두운 메모 카드 위 본문·보조 텍스트가 잘 보이지 않았음 | 다크모드 전용으로 `--text-secondary → #C7C7CC`, `--text-tertiary → #98989D`로 밝게 조정(라이트모드 불변). 메모 본문·발췌·메타데이터 가독성 향상 |

---

## [v0.7.0] — 2026-06-08 · 재고 그룹화 · 메모 순서 고정 · 고객사 관리

### ✨ 신규 기능

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 재고현황 품목코드별 그룹화 | 동일 품목코드 항목들이 개별 카드로 분산되어 사이즈·변형 구분이 어려웠음 | 동일 `attrs.code`를 가진 항목들이 하나의 그룹 카드로 묶이며, 그룹 헤더에 코드 배지·종류 수 표시. 그룹 내 각 항목(사이즈별)은 점 구분선으로 나열됨 |
| 위험 재고 정의 명확화 | "재고 부족" 뱃지가 음수 여부에만 표시되고 그룹 단위 경고가 없었음 | 위험 재고 기준 = qty < 0 (0개 미만). 개별 항목에 AlertTriangle 아이콘 + **위험 재고** 뱃지, 그룹 내 위험 항목이 있으면 그룹 헤더에 **위험 재고 포함** 경고 및 카드 테두리 빨간색 강조 |
| 메모 순서 고정 | 메모를 수정할 때마다 `updatedAt`이 갱신되어 목록 최상단으로 이동, 위치가 빈번히 뒤바뀜 | `id` 내장 생성 타임스탬프로 정렬 전환 — 수정해도 순서 불변. 고정 핀(📌) 항목은 여전히 최상단 우선 유지 |
| 고객사 관리 기능 추가 | 고객사 목록을 별도로 저장·관리하는 기능이 없었음 | `AppSettings.clients` 필드 추가. 설정 > 재고 마스터 설정 모달에 **고객사 관리** 섹션 신설 — 추가/삭제 가능한 배지 UI 제공 |
| 일정 고객사 연동 | 등록한 고객사를 일정에서 활용할 수단이 없었음 | 일정 등록·수정 폼에 고객사 선택 섹션 추가(설정에 고객사가 있을 때만 노출). 배지 클릭 또는 직접 입력으로 지정하며, 일정 카드에 고객사 배지로 표시됨 |
| 헤더 수동 동기화 버튼 | 서버 최신 데이터를 즉시 불러올 수단이 헤더에 없었음 | 상단 헤더에 새로고침 버튼 추가. 클릭 시 서버에서 최신 데이터를 즉시 다시 로드(동기화 중 스피너 표시, 중복 호출 방지) |

### ♻️ 변경

| 항목 | 내용 |
| --- | --- |
| 할 일 알림 기본값 | 새 일정 생성 시 `notifyOffset` 기본값이 `appSettings.defaultNotifyOffset ?? 0`으로 명시 보장됨 (설정 초기값 정각 = 0) |

---

## [v0.6.2] — 2026-05-29 · 복제 기능 & 설정 내 업데이트 버튼

### ✨ 신규 기능

| 항목 | 내용 |
| --- | --- |
| 일정·재고·메모 복제 | 카드 호버 시 나타나는 액션 버튼에 '복제' 버튼 추가. 누르면 동일 내용의 항목이 '(복사본)' 접미사와 함께 즉시 생성됨 |
| 설정 내 업데이트 버튼 | 설정 탭에 '지금 업데이트' 버튼 추가. Vercel 서버에서 최신 버전을 즉시 불러옴 |

---

## [v0.6.1] — 2026-05-29 · OS 배너 알림 · 동기화 · 헤더 업데이트 버튼

### ✨ 신규 기능

| 항목 | 내용 |
| --- | --- |
| OS 배너 알림 | 앱 실행 시 알림 권한 요청. 일정 알림 발생 시 인앱 카드 + 기기 OS 배너 알림 동시 표시 |
| 헤더 업데이트 버튼 | 상단 헤더에 새로고침 버튼 추가. 언제든지 최신 버전으로 즉시 업데이트 가능 |
| 컴퓨터·폰 데이터 동기화 | Electron 데스크톱 앱이 Vercel 서버(Neon DB)를 바라보도록 변경. 폰과 컴퓨터 앱 간 데이터 자동 동기화 |

---

## [v0.6.0] — 2026-05-29 · Vercel 배포 · Android APK · 버그 수정

### 🚀 배포 인프라

| 항목 | 내용 |
| --- | --- |
| Vercel 배포 | 앱을 Vercel 클라우드에 배포. URL: https://zero-friction-roan.vercel.app |
| Neon PostgreSQL | 로컬 DB에서 Neon 클라우드 DB로 전환. 어디서든 동일 데이터 접근 가능 |
| Android APK | Capacitor로 Android 네이티브 앱 빌드. 폰에 설치 후 일반 앱처럼 사용 가능 |
| Prisma 빌드 자동화 | Vercel 빌드 시 `prisma generate` 자동 실행되도록 build 스크립트 수정 |

### 🐛 버그 수정

| 항목 | 내용 |
| --- | --- |
| 카테고리 비율 툴팁 클리핑 | 스크롤 시 툴팁이 가려지는 문제를 `position: fixed` 방식으로 해결 |
| 인사이트 타일 레이블 줄바꿈 | '업무 진행 및 달성', '재고 건전성' 레이블이 두 줄로 깨지던 문제 수정 |

---

## [v0.5.10] — 2026-05-22 · 재고 일괄 등록 시 시리얼 번호 파싱 분류 고도화 및 리스트 페이지 삭제 시 페이징 바운더리 자동 보정

### 📦 재고 파싱 지능화 및 페이징 안정성 확보

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 1. `CLBX-5A-15689`과 같이 코드 뒤에 기기 일련번호(시리얼 번호)가 결합된 텍스트를 대량 등록창에 입력 시, 품목코드와 개별 기기를 구분하지 못하고 하나의 품목으로 간주하여 합산 또는 잘못 처리함.<br>2. 2열에 적힌 `"보유"`, `"동고FC"` 등의 자산 상태/소속 정보가 기기의 대표 품목명(`title`)으로 추출되어 다수의 서로 다른 기기가 동일한 상태 텍스트로 합쳐지거나 파편화됨.<br>3. 동일한 모델명이지만 고유한 시리얼 번호를 가진 서로 다른 실물 장비들이 대량 등록 시 하나로 병합(Merge)되어 수량이 뭉개지는 현상. | 1. **시리얼 번호 패턴 감지기(`isSerialPattern`)**를 도입하여 첫 열이 시리얼인 경우 품목코드(`CLBX-5A`)와 개별 고유 식별자(`serial`)로 자동 분리 추출합니다.<br>2. 2열에 매핑된 상태 단어(보유, 출고, 입고 등) 및 팀 명칭을 분류하여 품목명으로 지정하지 않고 **메모(`memo`) 필드로 자동 흡수/결합**되도록 개선했습니다.<br>3. 고유한 시리얼 번호 속성을 가진 행들은 **중복 품목명 병합(`mergeRows`) 대상에서 제외**하고 개당 수량 `1`개의 독립적 자산 카드로 개별 등록되도록 보장했으며, 일괄 등록 테이블에 **시리얼 프리뷰/수정 입력칸**을 신설했습니다. |
| 목록 페이지(일정, 재고, 메모)에서 상세 페이지를 통하지 않고 다이렉트로 삭제를 연속 진행했을 때, 삭제된 항목이 화면에서 바로 사라지지 않거나 마지막 페이지의 항목이 사라진 후 빈 화면(아웃 오브 바운드)에 멈춰 있어 사용자가 새로고침을 해야 하는 현상. | 각 항목 리스트의 전체 개수가 감소할 때 현재 표시 중인 페이지 번호가 유효 범위를 초과하는지 실시간 감지하여, 즉시 **마지막 유효 페이지로 교정(Clamp)해 주는 반응형 useEffect 동적 보정 로직**을 추가하여 새로고침 없이 즉각 목록이 지워지고 정렬되도록 개선했습니다. |

---

## [v0.5.9] — 2026-05-22 · 데이터베이스 중복 ID 자동 복구, 신규 생성 고유화 및 카드 액션 버블링 전면 차단

### ⚡ 데이터 무결성 및 리스트 상호작용 제어 강화

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 1. 다량의 항목을 빠른 시간 내에 복사/입력할 경우, 동일 밀리초(millisecond) 내에 등록되는 레코드들이 전부 동일한 ID(e.g., `asset_1779459570179`)로 생성되어 React 리스트 렌더링 시 키 중복 경고가 뜨고, 개별 삭제가 불가하거나 화면에서 바로 사라지지 않는 현상<br>2. 기존 로컬스토리지에 저장되어 버린 중복 ID 레코드들을 치유할 방법이 없어 브라우저 캐시를 지우지 않는 한 오작동이 누적됨 | 1. 레코드 추가(`addRecord`) 시 **고유한 7자리 무작위 문자열 접미사를 덧붙여** 절대 동일한 ID가 중복 발급되지 않도록 해결했습니다.<br>2. 데이터 로드(`getRecords`) 시점에 **중복 ID 여부를 자가 진단하여 실시간으로 다른 고유 ID를 부여하고 저장소에 다시 쓰는 자가 치유(Self-Repair) 루틴**을 내장하여 기존 사용자들의 데이터도 완벽하게 자동 보정되도록 복구했습니다. |
| 1. 리스트 카드에서 직접 수정/삭제 단추를 누를 때, 간격이나 단추 외곽 빈 곳을 클릭하면 상위 컨테이너 카드의 클릭 이벤트가 작동하여, 원치 않게 편집용 상세 모달 팝업이 전면에 열리며 삭제 조작을 방해함<br>2. 분류(Category) 상세 페이지 등 일부 보조 리스트 뷰에서 삭제 단추 클릭 시 `e.stopPropagation()` 처리가 누락되어 버블링이 일어남 | 1. 카드의 호버 액션 패널 영역(`.card-hover-actions`) 자체에 **`onClick={e => e.stopPropagation()}`을 부여하여** 패널 내부의 어떠한 빈 틈을 클릭해도 상위 카드로 클릭 이벤트가 전달되지 않게 차단했습니다.<br>2. 글로벌 CSS에서 `.card-hover-actions`에 **`z-index: 10` 레이어 우선순위를 부여**하여 카드 본문 텍스트나 뱃지 뒤로 파묻히는 터치 타겟 왜곡 현상을 해결하고, 모든 페이지 내 삭제 단추에 propagation 차단 처리를 완비했습니다. |

---

## [v0.5.8] — 2026-05-22 · 연속 삭제 조작성 및 토스트 차단 문제 완전 해결

### ⚡ 초고속 연속 삭제 및 UI 비차단 조작 보장

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 토스트 알림 표시 중에 그 뒤편의 삭제/수정 버튼 클릭이 차단되거나, 삭제 이후 아래에 있던 다른 항목 카드가 위로 밀려올 때 마우스를 강제로 흔들기(Hover 활성화) 전까지는 '삭제' 단추가 비활성 상태(`pointer-events: none`)로 유지되어 클릭 시 불필요하게 수정용 모달창이 터치되는 현상 | 1. 토스트 알림 컴포넌트에 **인라인 `pointer-events: none` 스타일을 다이렉트로 강제 적용**하여 브라우저의 스타일 캐시 오작동 가능성을 차단했습니다.<br>2. 리스트 카드 우측 하단 액션 단추(수정/삭제)를 기본 상태에서도 **약한 반투명(Opacity 0.35) 상태로 상시 노출하고 포인터 이벤트를 개방(`pointer-events: auto`)**하여, 마우스의 호버 활성화 딜레이나 흔들림 없이 연속해서 삭제 버튼만 즉시 연타할 수 있도록 개선했습니다. |

---

## [v0.5.7] — 2026-05-22 · 토스트 클릭 패스스루 및 재고 일괄 등록 Markdown 파싱 & 병합

### ⚡ 토스트 비차단화 및 일괄 처리 지능화

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 항목을 연속해서 지우고 싶어도 "삭제 완료" 등의 토스트 안내 메시지가 약 3초간 노출될 때 화면 하단을 물리적으로 가려서, 뒤편의 다른 삭제 단추나 탭 영역 클릭이 차단되어 작업 지연이 유발됨 | 토스트 요소에 `pointer-events: none` 스타일을 적용해 **알림 표시 중에도 뒤편의 모든 버튼과 리스트 영역을 자유롭게 클릭(마우스 통과)**할 수 있도록 수정하여 초고속 연속 삭제 등 조작 흐름이 끊기지 않게 개선했습니다. |
| Excel 형태의 탭/쉼표 데이터만 파싱이 가능하여, 챗봇 대화 내용이나 가이드 문서에서 복사한 Markdown 표 데이터(| 코드 | 품명 | 등)를 그대로 붙여넣으면 구조선과 헤더까지 전부 단순 텍스트로 잘려 들어가 오기입이 발생함 | 파싱 로직에 **Markdown 표 전용 구분자(|) 처리 및 구분선/헤더 건너뛰기 필터**를 추가하여, 복사한 마크다운 테이블을 통째로 입력창에 넣기만 해도 각 컬럼이 알아서 깔끔하게 정렬 및 입력되도록 대폭 확장했습니다. |
| 동일한 품목명을 가진 복수의 재고 변동 내역이 입력 데이터에 섞여 있을 때, 이를 각각의 별도 행으로 생성함으로써 중복 데이터가 쌓이고 최종 수량 가감이 파편화되었음 | 일괄 파싱 및 최종 제출 시점에 **동일 품목명을 기준으로 행을 하나로 묶고(Group & Merge), 입출고 구분에 따라 총합 수량을 정밀하게 산출**하며 개별 메모들을 세미콜론(;)으로 자동 결합해 단일 정규화된 데이터만 반영되도록 지능화했습니다. |

---

## [v0.5.6] — 2026-05-22 · 재고 일괄 처리 및 통합 메모 연동 기능 추가

### 📦 재고 여러 개 일괄 등록 및 변동 사항 메모 상호 연동

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 품목코드와 재고를 한 번에 여러 개 등록할 때, 매번 개별적으로 모달을 열어 수동으로 한 건씩 추가해야 하여 대량 등록 작업 시 입력 번거로움과 비효율성이 존재했음 | Excel/스프레드시트에서 **드래그하여 복사한 텍스트 데이터를 통째로 붙여넣으면 탭 및 쉼표(,) 구분자를 자동 분석하여 행으로 파싱**하는 일괄 등록 기능을 구현했습니다. |
| 파싱된 데이터를 확인하거나 수동으로 수정/삭제할 수 있는 임시 편집 레이어가 없었음 | 일괄 등록 모달 내에 **실시간 스프레드시트 형태의 편집 그리드 테이블**을 구성하여, 최종 등록 전 코드, 품목명, 수량, 구분(입고/출고), 보관위치, 담당자 및 메모를 자유롭게 인라인 수정/행 추가/삭제할 수 있게 개선했습니다. |
| 재고 대량 입력 시 작업 내역을 메모 등의 다른 도메인과 연동할 수 있는 링크 구조가 부재했음 | **"통합 변동 사항 메모 생성 및 상호 연동" 옵션**을 제공하여, 일괄 등록 처리 즉시 조정 품목과 수량 정보가 깔끔하게 담긴 마크다운 표 형태의 전용 메모가 자동 작성되고, 재고 카드와 메모 카드 간에 `linkedIds`를 통해 상호 링크 배지가 생기도록 구현했습니다. |
| 재고 리스트 카드에서 해당 재고 품목이 어떤 스케줄이나 메모와 연동되어 있는지 한눈에 알기 어려웠음 | 재고 목록 카드 내 상세 영역에 **연동된 메모의 제목을 해시태그 뱃지(#메모제목)로 노출**하여, 연동 관계를 시각적으로 즉시 확인 가능하게 했습니다. |

---

## [v0.5.5] — 2026-05-22 · 일정 카테고리 색상 선택 UI 리디자인

### 🎨 카테고리 색상 선택 서클 도트 및 피드백 개선

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 22px 크기의 너무 작고 단조로운 평면 색상 버튼을 사용하여 조작 편의성과 미관이 떨어졌음 | 크기를 **26px 프리미엄 원형 도트**로 20% 가까이 확대하여 시인성 및 터치 타깃 안정성을 확보함 |
| 선택 시 메인 테마의 파란색/보라색(`var(--accent)`) 링만 일률적으로 씌워져 카테고리 색상과 시각적 충돌이 있었음 | 선택 상태일 때 **해당 서클과 동일한 톤의 고유 색상 Concentric Halo Ring**을 감싸도록 입체 도트 방식으로 전면 개선함 |
| 버튼 호버 및 클릭 시 적절한 애니메이션 피드백이 없거나 부자연스러웠음 | 스프링 감각의 **Spring-like Scale 트랜지션 (`cubic-bezier(0.34, 1.56, 0.64, 1)`) 및 부드러운 그림자 확대 효과**를 내장하여 고급스러운 클릭 경험을 제공함 |
| 선택 완료 표시가 투박한 유니코드 텍스트 캐릭터 `✓` 로 제공되어 선명하지 못하고 어설펐음 | 고대비 **선명한 하이디피 SVG 체크마크 아이콘**으로 대체하고, 배경의 명도(Luminosity)를 자동 감지해 어두운 색에는 흰색 체크, 밝은 색(노랑 등)에는 검은색 체크를 가변 적용함 |
| 커스텀 컬러 피커가 Conic Gradient 원형 내부에 텍스트 `+` 로 투박하게 놓여 통일감이 결여되었음 | 미선택 시에는 **정교한 플러스 SVG 기호와 입체 그림자**를 부여하고, 커스텀 색상이 결정되면 해당 단색 배경과 동일색상 아우라 halo ring 및 SVG 체크마크가 연동되도록 리디자인함 |
| 상단 검색 및 퀵 입력창 placeholder의 단축키 힌트 및 부자연스러운 문장(`자연어로 등록하세요...` 등)이 포함되어 어색했음 | 한글 문장을 깔끔하고 보편적인 **`일정, 메모, 재고 입력...`** 및 올바른 영문 단축키 가이드 **`(Press / to focus)`** 로 통일하여 직관적이고 완성도 높은 디자인을 구현함 |

---

## [v0.5.4] — 2026-05-22 · 일정 카테고리별 커스텀 색상 지정 및 UI 동적 연동

### 🎨 일정 카테고리 커스텀 색상 지정 및 대시보드/일정 리스트 연동

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 새로운 일정 카테고리를 만들거나 기존 카테고리(업무, 회의, 개인, 일반 외)를 사용할 때 모두 보라색 등의 동일한 기본 색상으로만 지정되어 구분이 어려웠음 | 카테고리를 생성할 때 **9가지 기본 프리셋 색상 선택뿐만 아니라, 팔렛(Color Picker)을 통해 사용자가 원하는 커스텀 색상을 직접 지정할 수 있도록 추가**함 |
| 기존 카테고리 배지의 색상을 개별적으로 편집하는 UI가 부재하여 기존 카테고리 색상 변경이 번거로웠음 | 설정 모달 내 일정 카테고리 목록을 리스트로 구성하고, **각 카테고리 항목마다 개별 9가지 프리셋 선택 버튼과 직접 지정 컬러 피커를 제공하여 언제든 원하는 카테고리의 색상을 개별적으로 즉시 수정할 수 있도록 개선**함 |
| 설정의 일정 카테고리 목록 배지들이 전부 같은 기본 색상으로만 나열되었음 | 카테고리 목록에서 **각 카테고리에 지정된 커스텀 색상(연한 배경 틴트, 테두리, 전용 텍스트 색상 및 원형 불릿 점)으로 스타일링되어 개별 시각화**를 강화함 |
| 카테고리 색상 지정 변경 사항이 캘린더나 차트에 실시간으로 연동되지 않고 정적 상수로 처리되었음 | 지정된 색상 값이 메인 화면의 **일정 카테고리 구성 비율 차트 바, 캘린더 날짜별 일정 점(Dot), 일정 리스트 뱃지 및 카테고리 필터링 버튼에 동적으로 100% 반영**되도록 시스템 색상 맵핑 로직을 리팩토링함 |

---

## [v0.5.3] — 2026-05-22 · 일정 시간 입력 UI 일체화 및 상단 바 퀵 액션 단축 패널

### 📅 일정 등록 시간 입력 UI 통일 및 상단 바 단축 기능 강화

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 일정 등록 시 시간 입력창(CustomTimePicker)의 인풋 필드가 날짜 선택기(DatePicker)와 다른 텍스트 입력창 형태였으며, 선택 시 텍스트 파란색 반전 등 오작동과 깨짐이 존재했음 | 시간 선택기의 트리거 인풋을 **날짜 선택기와 동일하게 깔끔한 시계 아이콘 + 시간 표기 버튼 형태로 변경**하여 디자인 통일감을 확보함 |
| 일정 추가/수정 모달에서 "날짜"와 "시간"의 라벨 높이 차이로 인해 두 입력 박스의 상단 시작 수평선 위치가 어긋나 있었음 | 두 라벨의 높이를 **22px로 동일하게 맞추고 세로 정렬하여 날짜/시간 입력 박스가 한 수평선상에 깔끔하게 정렬**되도록 개선함 |
| 시간 설정의 직접 수동 타이핑 및 세부 변경을 위해 작은 인풋 창 내부에서 어렵게 수정해야 했음 | 직접 타이핑은 **시간 팝업 내부의 개별 입력 칸(시, 분)에서 안정적으로 입력 및 화살표 스텝 조절이 가능하도록 개선**함 |
| macOS 상단 바 트레이 아이콘을 왼쪽 클릭하면 자연어 입력창만 존재하는 1줄 형태의 드롭다운 패널이 떴으며 단축 기능이 없었음 | 입력창 하단에 **열기, 일정, 재고, 메모, 설정, 종료 단축 액션 버튼들이 포함된 글래스모피즘 2단 단축 패널**로 대폭 고도화함 |
| 상단 바 단축 패널이 어두운 단색 톤이었으며, 하단 기능 버튼의 디자인 시인성이 낮았음 | 단축 패널을 산뜻한 **라이트 모드(반투명 연회색)로 전면 전환하고, 하단 기능 버튼들을 깔끔한 테두리의 화이트 박스 스타일로 통일**하여 시인성을 극대화함 |
| 설정의 업데이트 정보 내역 및 상단 헤더에 v0.5.2 변경 사항이 최신으로 표시되었음 | 신규 변경 내용을 반영한 **v0.5.3 버전 업데이트 이력 추가 및 설정 업데이트 내역 최신화** 완료 |

---

## [v0.5.2] — 2026-05-22 · macOS 상단 바(트레이) 드롭다운 패널 연동 & 퀵 입력 포커스

### ✨ macOS 상단 메뉴 바(트레이) 드롭다운 연동 및 오토 포커스

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 트레이 아이콘 클릭 시 메인 윈도우가 일반 앱처럼 이전 활성 위치에 단순히 활성화/비활성화만 되었음 | 트레이 아이콘 클릭 또는 '열기' 시, 메인 윈도우가 **상단 바 바로 밑(가운데 정렬)에 드롭다운 패널 형태**로 자동 배치됨 |
| 상단 바를 통해 앱을 열어도 검색창(입력창)에 즉각 타이핑할 수 없어 수동으로 마우스 클릭이 필요했음 | 트레이 아이콘 클릭 즉시 **자연어 분석 입력창(`.command-input`)에 자동 포커스 및 전체 선택(Select All)**이 실행되어 바로 타이핑 가능 |
| 상단 메뉴 바의 트레이 아이콘 선이 얇아 macOS 기본 바에서 시인성이 떨어졌음 | Zero-Friction 브랜드 아이덴티티를 살린 **'0 안의 체크마크' 스타일의 굵고 선명한 신규 아이콘**(`trayTemplate.png`)으로 개편 |
| 일정 등록/수정 모달에서 시간 입력창(CustomTimePicker)의 Flex 레이아웃이 찌그러지거나 잘렸음 | Flex 레이아웃 대신 `display: block` 및 `box-sizing` 보정 스타일을 주입하여 **인풋이 잘리지 않는 정밀 38px 높이로 UI 교정** |
| 일정 등록 시 '하루 종일' 체크 토글 상태에 따라 시간 입력 부분의 크기/위치가 어색하게 변경됨 | 하루 종일 토글 시 '하루 종일' 텍스트 박스 높이도 **38px 및 border-radius 10px로 일치**시켜 모달 레이아웃 흐트러짐 제거 |
| 앱 창의 닫기(X) 버튼을 누르면 앱이 완전히 종료되었음 | 창을 닫아도 **백그라운드에서 상시 대기**하며, 상단 바 트레이 아이콘을 통해 원클릭 복원 가능 |
| 백그라운드 모드에서 앱을 완전히 끌 수 있는 간편한 UI 부재 | 트레이 아이콘 우클릭 컨텍스트 메뉴에 **'열기' 및 '종료'** 단추 탑재, 단축키 `Cmd+Q`를 통한 완전 종료 안정적 연동 |

---

## [v0.5.1] — 2026-05-22 · 알림 발송 신뢰성 수정 & 입력 편의성 개선

### 🐛 알림이 제시각에 표시되지 않던 문제 수정 & 편의성 추가

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 알림 타입·권한 상태에 따라 일정 시각에 인앱 카드가 뜨지 않는 경우가 있었음 | 타입/권한과 무관하게 일정 시각이 되면 **인앱 글래스모피즘 카드를 항상 표시** (발송 로직 통합) |
| '데스크톱 알림창' 타입에서 OS osascript 경고창과 인앱 카드가 중복 노출 | OS 다이얼로그를 제거하고 **인앱 카드로 일원화**, Electron은 `focus-window`로 창을 전면에 표시 |
| '테스트 실행'이 OS 경고창만 띄워 새 알림 UI를 확인할 수 없었음 | '데스크톱 알림창' 설정에서 테스트 시 **실제와 동일한 인앱 알림 카드를 즉시 미리보기** |
| 알림 팝업 울렸을 때 '완료'와 '스누즈' 버튼만 제공 | '확인' 버튼을 추가하여 **일정을 바로 완료 처리하지 않고 단순 닫기** 가능 |
| 시간 선택기(CustomTimePicker)의 텍스트 클릭 시 화살표 스텝 조작만 가능 | 텍스트 입력창을 클릭하여 **원하는 시간(HH:MM)을 직접 타이핑하여 입력**하고 다양한 간편 포맷 자동완성 및 유효성 검사 적용 |

> 'OS 배너 알림' 타입은 기존처럼 OS 레벨 배너(Electron 네이티브 / Web Notification / osascript)를 발송하며, 인앱 카드도 함께 표시됩니다.

---

## [v0.5.0] — 2026-05-22 · 데스크톱 알림 UI & 업데이트 내역 페이지네이션

### ✨ 커스텀 인앱 글래스모피즘 알람

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 일정 알림이 OS 기본 알림/다이얼로그에만 의존 | 화면 상단 중앙에 **글래스모피즘 알림 카드**를 framer-motion 슬라이드 애니메이션으로 표시 |
| 알림에서 후속 동작 불가 | 카드에서 **완료** · **10분 후 알림(스누즈)** · **닫기** 동작을 즉시 수행 |
| 스누즈 개념 부재 | `snoozedSchedules`로 일정별 다음 알림 시각을 메모리에 추적, 10분 뒤 재알림 |

### 🐛 데스크톱(Electron) 연동 보강

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 알람이 떠도 백그라운드 창이 앞으로 나오지 않음 | `'focus-window'` IPC로 알람 시 창을 **복원·포커스** |
| `osascript`를 문자열 보간으로 호출 → 따옴표/이스케이프 오류 위험 | **인자(argv) 기반 실행**(`on run argv`)으로 변경해 escape 오류 방지 |
| Electron 환경 감지가 불안정 | userAgent·process 체크로 `__IS_ELECTRON__` / `ipcRenderer`를 동적 바인딩 |

### ♻️ 설정 '업데이트 정보' 페이지네이션

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 버전 내역이 길게 나열된 **정적 목록** | `VERSION_LOGS` 데이터 배열 기반 **동적 렌더** |
| 모든 버전이 한 번에 노출되어 설정 패널이 길어짐 | **2개씩 페이지네이션**(이전/다음 + `n / m` 인디케이터) · 페이지 경계 보정 |

---

## [v0.4.4] — 2026-05-22 · 데이터 추출 지원, 반응형 최적화 및 페이지네이션 탑재

### ✨ 일정·재고·메모 데이터 추출 기능 추가 및 UI/UX 고도화

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 데이터를 외부 엑셀 문서나 인쇄물로 출력할 수 있는 방법이 부재했음 | 각 탭 및 독립적인 뷰어 페이지 상단에 '엑셀' 및 'PDF' 버튼을 추가하여 다운로드 및 인쇄가 가능하도록 개선 |
| 엑셀로 내보낼 때 한글이 깨지는 현상이 발생할 가능성이 있었음 | Excel 한글 깨짐 방지를 위해 UTF-8 BOM(\uFEFF)을 자동으로 추가하여 신뢰도 높은 CSV를 다운로드하도록 구현 |
| PDF로 인쇄할 때 원본 레이아웃이 뭉개지거나 스타일이 유실됨 | 보이지 않는 임시 iframe을 활용해 현대적이고 깔끔하게 스타일링된 출력 전용 문서를 동적으로 생성하여 인쇄(PDF 저장) 기능 실행 |
| 메모 내용에 포함된 마크다운 서식이 텍스트로 보임 | 메모 PDF 인쇄 시에도 인라인 마크다운 서식, 표, 코드 블록, 리스트, 인용구, 태그 및 멘션 등이 완벽히 렌더링되도록 경량 마크다운 파서 연동 |
| 정렬 규칙 없이 등록된 순서대로 추출됨 | 일정은 날짜/시간 오름차순, 재고는 카테고리/품명 오름차순, 메모는 고정 상태/날짜 내림차순으로 사전 정렬하여 내보내도록 편의성 강화 |
| 메모 상세 조회 모달에서 개별 메모를 별도로 파일 추출하거나 인쇄할 수 없었음 | 메모 상세 보기(읽기 전용 모드) 헤더에 엑셀 및 PDF 내보내기 전용 단추를 배치하여 단일 메모를 개별적으로 CSV 다운로드하거나 PDF 인쇄가 가능하도록 개선 |
| 화면 크기가 작아질 때 상단의 엑셀/PDF 내보내기 버튼의 크기가 뭉개지고 작아짐 | 엑셀 내보내기 버튼의 라벨명을 영어 'Excel'로 변경하고, 모든 내보내기 버튼(Excel & PDF)에 축소 방지 스타일(flexShrink: 0)을 부여하여 화면 크기와 무관하게 일정한 레이아웃 크기를 유지하도록 고도화 |
| 창 크기가 작아질 때 주요 타일, 인사이트 박스, 리스트 등의 레이아웃 크기가 멋대로 줄어들거나 글자가 찌그러짐 | 900px, 680px, 480px 화면 단위로 중단점(Breakpoints)을 명확하게 정의하여 좁은 창에서도 각 요소를 찌그러뜨리지 않고 라벨 단축, 탭 아이콘화, 2x2 및 1열 그리드 감싸기 동작을 적용함 |
| 일정 캘린더 페이지 하단의 일정 목록 및 독립적인 재고 관리 페이지에서 페이지네이션이 부재하여 목록이 길어지면 아래로 길게 늘어남 | 캘린더 일일 일정 리스트와 재고 대장 관리 서브페이지에 홈 화면 탭들과 동일하게 페이지네이션(5개 단위)을 완벽히 탑재하고, 품목 추가/삭제 시에도 페이지 인덱스가 유연하게 갱신되도록 상태를 최적화함 |

---

## [v0.4.3] — 2026-05-22 · 일정 옵션 개선 및 메모 마크다운 확장

### ✨ 일정 '하루 종일' (All Day) 옵션 지원

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 모든 일정에 특정 시간을 필수 지정해야 하여 하루 종일 이어지는 이벤트를 표현하기 어려움 | 일정 등록/수정 모달에 '하루 종일' 토글 스위치 제공. 활성화 시 시간 선택 창이 숨겨지며 '하루 종일' 배지로 대체됨 |
| 일정 리스트에서 시간순 정렬만 지원하여 시간 없는 일정이 뒤섞임 | 하루 종일 일정(`attrs.allDay === true`)을 당일 리스트의 가장 최상단에 우선 정렬 |
| 대시보드 및 일정 목록에 무조건 시간 텍스트만 표시됨 | 대시보드 리스트와 달력 일일 목록에 각각 '하루 종일' 또는 '종일' 배지 노출 |
| 백업 원본 데이터나 CSV 파일에 시간만 표시되거나 알림 체크 대상에 포함됨 | CSV 내보내기 시 '하루 종일'로 출력되며, 백업 탐색기에도 명시. 백그라운드 알림 발생 조건에서 자동 배제 |

### ♻️ 완료된 일정의 달력 표시 유지

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 일정을 완료 처리하면 달력 월간 그리드 셀 하단의 작은 점(일정 표시 점)이 즉시 사라짐 | 일정이 완료 처리되더라도 달력 날짜 셀의 일정 표시 점이 유지되어 한 달간의 전체 스케줄 흐름을 파악하기 용이하도록 개선 |

### ✨ 메모 본문 마크다운 테이블(표) 및 코드 블록 지원

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 메모 본문 렌더링 시 표준 마크다운 표(`|` 문자 조합)가 일반 텍스트로 풀어져 알아보기 어려움 | 마크다운 표 표기법 및 정렬 문법(예: `:---`, `:---:`, `---:`)을 해석하여 깔끔한 HTML 표로 렌더링. 테마에 대응하는 보더와 가로 스크롤 최적화 적용 |
| 여러 줄 코드(` ``` ` 펜스 형태) 작성 시 줄바꿈이 무시되고 인라인 코드 스타일로 뭉개짐 | 펜스 코드 블록을 감지해 전용 스크롤 상자 및 우측 상단 언어 라벨 배지를 포함한 코드 블록 레이아웃으로 렌더링 |

---

## [v0.4.2] — 2026-05-22 · 시간 선택기 개선 및 터미널 팝업 제거

### ♻️ 시간 선택기(CustomTimePicker) UI/UX 개선 (Stepper 전용 설계)

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 3D 스크롤 휠 방식으로 데스크톱 마우스/트랙패드에서 스크롤 조작이 불안정하고 모달이 의도치 않게 닫힘 | 아크릴 글래스모피즘이 적용된 초소형(가로 190px, 세로 140px) 팝업 제공. 24시간제 디지털 표시창과 상/하 방향 단추(`▲`/`▼`)만으로 깔끔하게 조작 가능 |
| 시간 선택창의 그리드 선택기(시/분 숫자 버튼들)로 인해 다소 복잡했던 레이아웃 | 그리드 숫자판을 전부 삭제하여 미니멀한 UI로 간소화 |
| 시간 선택창을 클릭할 때 부모 모달이 닫히는 현상 | `e.stopPropagation()` 처리로 이벤트 버블링 방지 |

### 🐛 Electron 데스크톱 앱 실행 시 터미널 중복 실행 제거

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| `ZeroScheduler.app` 앱 실행 시 백그라운드 셸 스크립트 실행을 위해 빈 터미널(`Terminal.app`) 창이 매번 새로 열려 활성화됨 | AppleScript 실행기(`main.scpt`)에서 `tell application "Terminal"` 코드를 제거하여 터미널 창 노출 없이 앱만 무소음으로 기동하도록 수정 |

---

## [v0.4.1] — 2026-05-21 · 재고 삭제 UX 개선 & 기본 알림 정각 설정

### 🐛 재고 삭제 시 모달 즉시 닫힘 및 버블링 방지

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 재고 상세 페이지에서 재고를 삭제해도 모달이 열려 있거나, 목록에서 삭제 시 클릭 이벤트 버블링으로 인해 모달이 다시 열림 | 상세 모달에서 삭제 시 즉시 닫히고, 목록에서 삭제 시 `e.stopPropagation()`을 호출하여 불필요하게 상세 모달이 뜨는 현상 방지 |

### ♻️ 일정 등록 시 기본 알림 설정 "정각(0분)"으로 변경

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 일정을 새로 등록하거나 기본 알림을 설정할 때 초기값이 10분 전으로 설정됨 | 기본 설정을 정각(0분)으로 수정하고, 모달 및 캘린더, 커맨드 팔레트 등 일정 생성 영역 전반에 반영 |

---

## [v0.4.0] — 2026-05-21 · UI/UX 개선 & 문서화 *(현재/미배포)*

### ✨ 메모 상세보기 — 보기/수정 모드 분리

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 메모 클릭 시 **곧바로 편집 화면**이 열림 | **읽기 전용 상세보기**로 먼저 열리고, 우측 상단 **"수정"** 버튼을 눌러야 편집 모드로 전환 |
| — | 신규 메모(+버튼)·카드의 "수정" 버튼은 기존처럼 곧바로 편집 모드 진입 |

### ♻️ 메모 리스트 카드 크기 통일

| 변경 전 (Before) | 변경 후 (After) |
| --- | --- |
| 내용 길이에 따라 카드 높이가 **제각각** | 모든 카드 **150px 고정** + 초과 내용 클리핑으로 균일한 그리드 |

### ♻️ 입력 폼 마이크로 카피·크기 정리

| 항목 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 일정 메모 입력창 | `rows={2}` | `rows={4}` |
| 재고 메모 입력창 | `rows={3}` | `rows={5}` |
| 재고 라벨 | "특이사항" | "메모" |
| 품목코드 플레이스홀더 | "예: CODE-01" | (제거) |
| 카테고리 옵션 라벨 | "카테고리 선택" | (제거) |
| `CustomSelect` 기본 표시 | "선택..." | (빈 값) |

### 📝 문서

* `zero_friction_fullstack_master_guide.md`의 저장 모델 서술을 **하이브리드(명시적 저장 + 자동 영속화)** 로 정정, **Cmd+F 커맨드 팔레트** 섹션·단축키 치트시트·Maintainer 표기 추가
* `CHANGELOG.md`(Before→After), `CONTRIBUTING.md`(커밋·푸시 규칙) 신규 작성
* 🗑️ `README.md` 제거 — 문서를 마스터 가이드로 일원화

---

## [v0.3.0] — 2026-05-21 · 코드 품질 & `src/` 아키텍처

### ♻️ 코드 리팩토링

대상: `ClientLayout` · `AppContext` · `SettingsSection` · `app/page.tsx`

| 구분 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 미사용 코드 | 죽은 함수·미사용 import/변수 잔존 (`getActivityBgColor`, `handleSaveSettings` 체인, `useRef`/`createPortal` 등) | 전부 제거 — 런타임 DOM 동일 |
| 중복 로직 | 동일 구조 4개 네비 버튼, 카테고리 색상 `switch` 3벌, 활동색 if 체인 | 데이터 기반 `map`/룩업 테이블로 통합(DRY) |
| 문서화 | JSDoc·주석 거의 없음 | 주요 컴포넌트·함수에 JSDoc + 한국어 인라인 주석 |

### 🏗️ `src/` 디렉터리 통합

| 구분 | 변경 전 (Before) | 변경 후 (After) |
| --- | --- | --- |
| 디렉터리 | 루트에 `app/ frontend/ backend/ database/ lib/ worker/` 산재 | `src/` 하위로 6개 폴더 통합, 운영 스크립트는 `scripts/`로 격리 |
| 경로 별칭 | `@/* → ./*` | `@/* → ./src/*` (import 구문은 **한 줄도 미수정**) |
| 루트 보존 | — | `public/` · `prisma/` · `main.js`는 생태계 규약상 루트 유지 |
| PWA 설정 | `customWorkerDir: "worker"` | `customWorkerDir: "src/worker"` (`dest: "public"` 유지) |

### 📝 문서

* `zero_friction_fullstack_master_guide.md` 신규 작성 (제품 비전·PWA·아키텍처·온보딩)

---

## [v0.2.0] — 2026-05-20 · 커스텀 UI 컨트롤 & 메모 고도화

### ✨ 추가 (Added)

* **커스텀 폼 컨트롤** 도입: `CustomSelect`, `CustomTimePicker`, `CustomDatePicker` — OS·브라우저 기본 위젯 대신 일관된 디자인의 드롭다운/피커
* **마크다운 메모**: `Markdown` 컴포넌트로 메모 본문 렌더링(체크리스트·강조 등)

### ♻️ 변경 (Changed)

* 캘린더·재고·설정 화면을 위 컨트롤 기반으로 재구성하고 표시 옵션(밀도·노출 개수 등) 정교화

---

## [v0.1.0] — 2026-05-19 · 초기 구축 (Foundation)

### ✨ 추가 (Added)

* **앱 골격**: Next.js 16(App Router) 기반 `Zero-Friction` 위젯 — 전체/일정(캘린더)/재고/메모/설정 단일 화면 전환
* **통합 데이터 모델**: 일정·재고·메모를 단일 `UniversalRecord`로 관리하는 `database` 모듈, 로컬 우선(LocalStorage) 영속화
* **백엔드 서비스**: 자연어 파싱(LLM: Gemini + 로컬 폴백 파서), 알림·정책·스케줄 서비스 및 `/api/*` 라우트
* **데스크톱 셸**: Electron `main.js` — 420×850 프레임리스 위젯, 알림 IPC
* **PWA**: `manifest.json`(standalone·portrait·maskable), 커스텀 서비스 워커(`worker/index.ts`)의 Web Push 수신 및 알림 클릭 시 기존 창 포커스, `useNotifications` 훅

---

## 범례

* ✨ 추가(Added) · ♻️ 변경(Changed) · 🐛 수정(Fixed) · 🗑️ 제거(Removed) · 📝 문서(Docs) · 🏗️ 아키텍처
* 날짜 형식: `YYYY-MM-DD` · 시간대: KST
* 과거 버전 구분은 커밋 메시지가 비어 있던 초기 히스토리를 변경 파일·규모로 분석해 재구성했습니다.

*Maintainer: **screening25***
